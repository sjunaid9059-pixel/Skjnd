# Firebase Merge Panel v3

Primary input is a plain Firebase RTDB URL:
`https://lodaroll-default-rtdb.firebaseio.com`

Also accepts the previous MERGED::: Base64 `?s=` links.

Deploy every file in this folder to the root of your GitHub repo and redeploy on Vercel.